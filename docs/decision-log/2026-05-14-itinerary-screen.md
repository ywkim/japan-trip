# 2026-05-14 — 일자별 상세 일정 화면 + 단일 출처 분리

## 트리거

교토 5/31~6/3 + 시오·카덴쇼 확정. 일정이 굳어진 만큼 시부모/부부 공유용 일자별 상세 화면이 필요. 기존 `index.html`의 8섹션 카드 중 §6 일정은 요약 수준이라 시간대·메모·도보거리까지 보려면 별 화면이 필요.

## 산출물

- `data/itinerary.json` — 신규 단일 출처. 4일(5/31, 6/1, 6/2, 6/3) × 각 시간 블록(시간대·동선·메모) + 일자별 도보거리(km) + 여행 메타(목적지·일자·인원·숙박) + 보류 사항.
- `viz/itinerary.html` — 신규 시각화. `data/itinerary.json`을 인라인으로 가져와 더블클릭 동작. 일자 탭 4개 + 일자별 시간대 표 + 도보거리 막대 + 보류 사항.
- `README.md` — 사용법 §3에 `viz/itinerary.html` 1줄 추가.
- `CLAUDE.md` — 디렉토리 트리 + 단일 출처 목록 + `viz/itinerary.html` 수동 sync 주석.

## 합의

- `docs/kyoto-itinerary-may31-jun3-2026.md`(사람용 마크다운)는 그대로 유지하고, 구조화 데이터 단일 출처를 `data/itinerary.json`으로 분리.
- 화면(`viz/itinerary.html`)은 외부 fetch 없이 인라인 데이터로 더블클릭 동작 보장 — `viz/dashboard.html`과 같은 규약.
- 일자 전환은 단순 탭 (가중치 슬라이더 같은 복잡 인터랙션 없음).

## 핵심 관찰

- 단일 출처 일관성: 마크다운 사람용 사본은 보존, 구조화 데이터는 JSON으로 분리해 화면·향후 빌드 도구가 같은 출처를 참조.
- 모바일 사용성: 탭 → 단일 카드 표시. 시간대 컬럼은 좁은 화면(<480px)에서 글자/패딩 축소.
- 사람용 vs 데이터 분리: 네이버 블로그 후기 URL 리스트는 마크다운 사본에 유지(검색 URL이라 데이터 정형 가치 낮음). JSON에는 일정 코어만.
- 도보 합계 13km — 빡빡하지만 시부모 견딜 범위. 일자별 막대 시각화로 한눈에 확인 가능.

## 보류

- `viz/itinerary.html`은 아직 `build_index.py` 빌드 대상이 아님 → `data/itinerary.json` 수정 시 인라인 데이터를 수동 sync (CLAUDE.md TODO 명시).
- 식당·체험 사전 예약(시그라쿠 토후 4인 좌석·카덴쇼 가이세키 시간)은 별 일지에서 다룸.

## 다음 단계

1. 영욱·소연 검토 → 시간대/메모 미세 조정 (필요 시 JSON 수정 + HTML 인라인 동기화).
2. 식당·체험 사전 예약 일지 작성 (시그라쿠·카덴쇼 가이세키).
3. `build_index.py`에 `viz/itinerary.html` 빌드 통합 (현재 `index.html`만 빌드 대상 → 동일 패턴으로 확장).
