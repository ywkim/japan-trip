# 2026-05-17 — Playwright MCP 후속 세션 위임 가이드 신설

## 트리거

PR #29(`feat(itinerary): 장소 간 이동 출처·신뢰도 라벨링 + 검사 G`)에서 도입한 신규 `data_quality` 라벨 `tbd_needs_browser_mcp`. WebFetch가 Google Maps Directions의 JS 렌더링을 처리 못 해 측정 못 한 7~8 leg가 이 라벨로 남아있음. 이를 후속 세션에 인계하려면 **세션이 본 대화 컨텍스트 없이도 작업을 시작할 수 있는 자기완결 문서**가 필요.

## 산출물

- `docs/transit-mcp-handoff.md` — 후속 세션 위임 가이드. 환경 요구사항(Playwright MCP), 처리 대상 leg 표, grep으로 정확 위치 찾기, 측정 절차 6단계, before/after 편집 예시, 검증 명령, 마크다운 사본 동기화, 결정 일지 템플릿, 커밋·PR 가이드 포함.
- `CLAUDE.md` 디렉토리 구조 트리에 추가.
- `README.md` 보조 데이터 섹션에 1줄.

## 핵심 관찰

- **자기완결성 원칙**: 인계 문서가 "지난 PR을 참고하라" 식 의존이면 후속 세션이 막힘. 측정 URL·필드 이름·승격 라벨·검증 명령을 모두 한 파일에 박아두는 게 핵심.
- **검사 G의 walking_km 검증 방향**: leg 합이 declared `walking_km`을 +2km 이상 초과할 때만 fail (한 방향). 후속 세션이 도보 leg를 늘려서 declared를 초과하면 days[].walking_km도 같이 올려야 한다는 점을 인계 문서에 명시.
- **라벨 승격 경로**: `tbd_needs_browser_mcp` → 공식 운임 페이지(JR/교토 시영) 확인 시 `official_fare`, Maps/시세 사이트 측정이면 `researched_market_rate`. tbd는 60d staleness 검사 면제.

## 보류 / 다음 단계

- 후속 세션에서 측정 완료 시: 새 PR(별도 브랜치 `claude/transit-mcp-measurements` 등) → data/itinerary.json·docs/kyoto-itinerary-may31-jun3-2026.md·index.html·viz/itinerary.html 동기 갱신 → `docs/decision-log/YYYY-MM-DD-transit-mcp-measurements.md` 신규 일지.
- 인계 문서 §10에 추가 위임 후보 명시: arrive_from에 `fare_yen` 필드 확장, validate.py 검사 H(itinerary MD↔JSON) 신설.
