# 2026-05-11 — 항공권 데이터 v2.1 (소연 후속 처리 4건 반영)

- 트리거: 소연 PR #6 v2 리뷰(DISMISSED, 본문 승인 톤)에서 머지 후 후속 처리 5건 제안. 사용자 결정: 이번 PR에서 모두 처리(family_fit 가중치만 별도 PR).
- 정책 충족: PR #7 "실시간 가격 리서치 우선" 강제 규칙(`source`·`data_quality`·환율) 만족.

## 변경

- `data/flights.json`:
  - 노선별 `source` 필드 추가 — 메타사이트명·일자·근거 가격 명시 (6개 노선 전체)
  - 노선별 `data_quality` 라벨 추가 — 모두 `researched_market_rate` (ICN-KIX만 confirmed_booking 1건 교차검증 명시)
  - `data_quality.default_label`·`label_meaning` 신설
  - `fx_assumption` 신설 — JPY 9.5 / USD 1380 (2026-05-09 기준)
  - `policy.lcc_4pax_markup_pct.derivation` 신설 — PR #2 에어서울 ₩1,170,638 표본 분석 근거 1줄
  - 노선별 `queried_at`을 2026-05-09(실제 조회일)로 정정 (v2에서 잘못 2026-05-11로 표기)
- `data/decision.json`·`viz/dashboard.html`:
  - `travelers`의 `"시부"`/`"시모"` → `"시아버지"`/`"시어머니"` (소연 nice-to-have)
- `docs/flights.md`:
  - v1→v2→v2.1 변경표 갱신
  - §6.5 마크업률 도출 근거 신설
  - §6.6 환율 가정 신설

## 보류 (별도 PR 권장)

- `family_fit` 가중치 0.10 → 0.15 증액 — 의사결정 자체에 영향, 부부 합의 후 별도 PR

## 주요 관찰

- v2.1은 **수치 변경 없음**. 데이터 품질·추적성·정책 충족만 강화.
- confirmed_booking 표본은 현재 1건(에어서울 ICN-KIX). 부킹 진행 시 라벨이 점진적으로 `researched_market_rate` → `confirmed_booking`으로 승격.
- 마크업률 15/25/35는 단일 표본 기반 보수 추정 — 부킹 사례 누적에 따라 후속 PR에서 재추정.

## 다음 단계

- 도시 후보 단축 합의 → 해당 노선 공식 사이트 직접 4인 검색 → confirmed_booking 라벨로 승격
- `family_fit` 가중치 별도 PR 발의
