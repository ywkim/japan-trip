# 2026-05-09 — 의사결정 일지 분리 (충돌 폭발 방지)

- 배경: PR #4·#5·#6가 모두 `dirty` 상태. 머지 시뮬레이션 결과 충돌 파일은 거의 모두 `docs/decision-log.md` 1개. PR #6는 `reports/final-report.md`도 추가로 충돌.
- 핵심 관찰:
  - 모든 일지 PR이 같은 베이스(`ee577fd`)에서 분기되어 `docs/decision-log.md` **EOF에 append** 하는 동일 패턴 사용.
  - PR #2가 먼저 머지되면서 EOF 위치 변동 → 다른 PR들의 unified-diff 컨텍스트가 깨져 자동 머지 실패.
  - 논리적 충돌은 아님 (서로 독립적인 일지 항목 추가). 그러나 매 머지마다 비용 발생 → 누적 폭발.
  - 단순 "인덱스 파일"로 분리해도 인덱스 자체가 같은 패턴의 충돌을 다시 만든다.
- 합의: **항목 1개 = 파일 1개** 구조로 전환.
  - `docs/decision-log/` 디렉토리에 `YYYY-MM-DD-slug.md` 단위로 추가.
  - 디렉토리 자체가 인덱스 (별도 인덱스 파일 없음).
  - 새 PR은 새 파일만 추가 — 기존 파일을 편집하지 않는다.
- 변경:
  - 기존 일지 3건을 개별 파일로 분리 (`2026-05-09-repo-init.md`·`-weather.md`·`-kyoto-itinerary.md`)
  - `docs/decision-log.md` 삭제, 컨벤션 가이드는 `docs/decision-log/README.md`로 이전
  - `CLAUDE.md` · `README.md`의 일지 참조 경로·메타 규칙 갱신
- 영향:
  - 열린 PR #4·#5·#6·#7은 모두 리베이스 필요. 각 PR의 일지 추가분을 **새 파일**로 옮기면 자동 머지 가능.
  - PR #6의 `reports/final-report.md` 충돌은 별개 — 동일 라인을 PR #2와 동시 수정한 진짜 라인 충돌이라 수동 해소 필요.
- 다음 단계:
  - 본 변경 머지 후 PR #4·#5·#6·#7 작성자에게 리베이스 가이드 안내
  - `reports/final-report.md` 같은 "최종 산출물" 파일도 동일 충돌 패턴이 재발하면 분할 검토
